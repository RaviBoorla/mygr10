{
  "name": "mygr10",
  "version": "1.0.0",
  "private": true,
  "description": "Practice and self-assessment app for Indian Grade 10 board exam students (CBSE / ICSE / IB).",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "echo 'Static site — no build step required'",
    "dev": "npx serve public",
    "deploy": "wrangler pages deploy public"
  },
  "engines": {
    "node": ">=18"
  }
}
